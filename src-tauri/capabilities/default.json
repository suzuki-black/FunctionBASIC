{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "エディタのデフォルト権限",
  "windows": ["main", "music"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "dialog:default",
    "opener:default"
  ]
}
