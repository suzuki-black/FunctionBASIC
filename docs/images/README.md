# README screenshots

Add the two screenshots referenced by the top-level `README.md` here:

- `convert.png` — the editor in split view: Structured BASIC source on the left,
  generated MSX-BASIC on the right (the "Convert" screen).
- `run-webmsx.png` — the converted program running inside the embedded webMSX
  ("Run (WebMSX)" tab), e.g. showing the sample program's output.

Recommended width: ~1400px. PNG.
